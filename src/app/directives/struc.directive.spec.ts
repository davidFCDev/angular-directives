import { StrucDirective } from './struc.directive';

describe('StrucDirective', () => {
  it('should create an instance', () => {
    const directive = new StrucDirective();
    expect(directive).toBeTruthy();
  });
});
